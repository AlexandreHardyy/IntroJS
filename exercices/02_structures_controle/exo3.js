/**
 * Exercice 3 : Boucle For
 * Consigne : Créez une fonction `sommeJusqua` qui prend un nombre `n` en paramètre.
 * Elle doit retourner la somme de tous les entiers de 1 à `n` (inclus).
 * Exemple : sommeJusqua(3) -> 1 + 2 + 3 = 6
 */

export function sommeJusqua(n) {
    // Écrivez votre code ici
    return 0;
}
