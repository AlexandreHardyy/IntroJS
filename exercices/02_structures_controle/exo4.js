/**
 * Exercice 4 : Boucle et Conditions
 * Consigne : Créez une fonction `compterVoyelles` qui prend une chaîne de caractères en paramètre.
 * Elle doit retourner le nombre de voyelles (a, e, i, o, u, y) contenues dans la chaîne (en ignorant la casse).
 */

export function compterVoyelles(chaine) {
    // Écrivez votre code ici
    return 0;
}
