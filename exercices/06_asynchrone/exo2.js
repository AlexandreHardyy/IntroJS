/**
 * Exercice 2 : Async/Await
 * Consigne : Créez une fonction `recupererDonnees`.
 * Elle doit attendre 1 seconde (simulé) puis retourner la chaîne "Données reçues".
 * Utilisez async/await et la fonction `attendre` de l'exercice 1 (si possible, ou réimplémentez le délai).
 */

export async function recupererDonnees() {
    // Écrivez votre code ici
    return "";
}
