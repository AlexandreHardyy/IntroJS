// Exercice 1 : Manipulation de chaînes de caractères

// 1. Écrivez une fonction `extraireExtension` qui prend un nom de fichier (ex: "image.png")
// et retourne l'extension (ex: "png").
export function extraireExtension(fichier) {
    // TODO
    return "";
}

// 2. Écrivez une fonction `nettoyerEmail` qui prend un email avec des espaces
// et en majuscules (ex: "  User@Example.com  ")
// et retourne l'email propre en minuscules (ex: "user@example.com").
export function nettoyerEmail(email) {
    // TODO
    return "";
}

// 3. Écrivez une fonction `compterMots` qui prend une phrase
// et retourne le nombre de mots (séparés par des espaces).
export function compterMots(phrase) {
    // TODO
    return 0;
}
