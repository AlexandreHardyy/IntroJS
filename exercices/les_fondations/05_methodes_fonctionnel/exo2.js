// Exercice 2 : Transformation avec .map()

// 1. Écrivez une fonction `doublerNombres` qui prend un tableau de nombres
// et retourne un nouveau tableau avec tous les nombres doublés.
export function doublerNombres(nombres) {
    // TODO
    return [];
}

// 2. Écrivez une fonction `nomsUtilisateurs` qui prend un tableau d'objets utilisateurs
// (ex: [{ nom: "Alice", age: 25 }, { nom: "Bob", age: 30 }])
// et retourne un tableau contenant uniquement les noms (ex: ["Alice", "Bob"]).
export function nomsUtilisateurs(utilisateurs) {
    // TODO
    return [];
}
