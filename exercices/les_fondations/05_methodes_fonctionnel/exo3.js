// Exercice 3 : Sélection avec .filter()

// 1. Écrivez une fonction `nombresPairs` qui ne garde que les nombres pairs d'un tableau.
export function nombresPairs(nombres) {
    // TODO
    return [];
}

// 2. Écrivez une fonction `produitsPasChers` qui prend une liste de produits
// (ex: [{ nom: "Lait", prix: 10 }, { nom: "Caviar", prix: 1000 }])
// et retourne ceux qui coûtent moins de 50.
export function produitsPasChers(produits) {
    // TODO
    return [];
}
