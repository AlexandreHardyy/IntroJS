/**
 * Exercice 2 : Logique
 * Consigne : Créez une fonction `estPalindrome` qui prend une chaîne de caractères.
 * Elle retourne `true` si la chaîne est un palindrome (se lit de la même façon dans les deux sens), `false` sinon.
 * Ignorer la casse (majuscule/minuscule).
 */

export function estPalindrome(mot) {
    // Écrivez votre code ici
    return false;
}
