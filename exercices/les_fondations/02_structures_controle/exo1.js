/**
 * Exercice 1 : Condition Check
 * Consigne : Créez une fonction `estPair` qui prend un nombre en paramètre.
 * Elle doit retourner `true` si le nombre est pair, et `false` s'il est impair.
 */

export function estPair(nombre) {
    // Écrivez votre code ici
    return false;
}
