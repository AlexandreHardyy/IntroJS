/**
 * Exercice : Propriétés Privées et Accesseurs (Getters/Setters)
 * Consigne : Créez une classe `CompteBancaire` avec une propriété privée `_solde` (initialisée via le constructeur).
 * Ajoutez un getter `solde` pour récupérer la valeur.
 * Ajoutez un setter `solde` qui met à jour la valeur UNIQUEMENT si le nouveau montant est positif.
 */

export class CompteBancaire {
    // Écrivez votre code ici
}
