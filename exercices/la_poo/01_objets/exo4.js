/**
 * Exercice 4 : Modification
 * Consigne : Créez une fonction `ajouterPropriete` qui prend un objet, une clé (string) et une valeur.
 * Elle ajoute la propriété à l'objet (ou la modifie si elle existe déjà) et retourne l'objet modifié.
 */

export function ajouterPropriete(objet, cle, valeur) {
    // Écrivez votre code ici
    return objet;
}
