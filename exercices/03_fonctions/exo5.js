/**
 * Exercice 5 : Boucles et Logique
 * Consigne : Créez une fonction `estPremier` qui prend un nombre entier.
 * Elle retourne `true` si le nombre est premier, `false` sinon.
 * Note: Un nombre premier n'est divisible que par 1 et lui-même. 1 n'est pas premier.
 */

export function estPremier(n) {
    // Écrivez votre code ici
    return false;
}
