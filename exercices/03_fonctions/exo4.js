/**
 * Exercice 4 : Calcul
 * Consigne : Créez une fonction `calculerRemise` qui prend un prix et un pourcentage de remise.
 * Elle retourne le nouveau prix après application de la remise.
 */

export function calculerRemise(prix, pourcentage) {
    // Écrivez votre code ici
    return 0;
}
