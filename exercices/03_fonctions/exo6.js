/**
 * Exercice 6 : Récursion (ou boucle)
 * Consigne : Créez une fonction `fibonacci` qui prend un entier `n`.
 * Elle retourne le n-ième terme de la suite de Fibonacci.
 * Fib(0) = 0, Fib(1) = 1, Fib(n) = Fib(n-1) + Fib(n-2)
 */

export function fibonacci(n) {
    // Écrivez votre code ici
    return 0;
}
