/**
 * Exercice : Créer une promesse
 * Consigne : Écrivez une fonction `attendre` qui prend un nombre de millisecondes en paramètre.
 * Elle doit retourner une nouvelle `Promise` qui se résout après le délai indiqué (en utilisant `setTimeout`).
 * La promesse pourra par exemple se résoudre avec le message "Attente terminée".
 */

export function attendre(millisecondes) {
    // Écrivez votre code ici
}
