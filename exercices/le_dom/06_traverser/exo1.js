/**
 * Exercice 1 : Traversée d'arbre DOM
 * 
 * Consigne : Implémentez des fonctions de traversée d'arbre.
 * 
 * Un nœud a la structure : { tag, id, classes: [], text: '', children: [] }
 */

/**
 * 1. Écrivez une fonction `compterNoeuds` qui prend un arbre (nœud racine)
 * et retourne le nombre total de nœuds (incluant la racine et tous ses descendants).
 */
export function compterNoeuds(noeud) {
    // Écrivez votre code ici
    return 0;
}

/**
 * 2. Écrivez une fonction `profondeurMax` qui prend un arbre
 * et retourne la profondeur maximale de l'arbre.
 * Un nœud seul (sans enfants) a une profondeur de 1.
 */
export function profondeurMax(noeud) {
    // Écrivez votre code ici
    return 0;
}

/**
 * 3. Écrivez une fonction `collecterTextes` qui prend un arbre
 * et retourne un tableau de tous les textes non vides trouvés
 * dans l'arbre, en parcourant en profondeur (depth-first, pré-ordre).
 */
export function collecterTextes(noeud) {
    // Écrivez votre code ici
    return [];
}

/**
 * 4. Écrivez une fonction `aplatir` qui prend un arbre
 * et retourne un tableau contenant TOUS les nœuds de l'arbre
 * dans l'ordre d'un parcours en profondeur (pré-ordre).
 * Chaque nœud dans le résultat est l'objet original (pas une copie).
 */
export function aplatir(noeud) {
    // Écrivez votre code ici
    return [];
}
