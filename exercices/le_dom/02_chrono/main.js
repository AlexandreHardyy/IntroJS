/**
 * EXERCICE : CRÉATION D'UN CHRONOMÈTRE
 * 
 * Objectifs :
 * 1. Sélectionner les éléments du DOM : l'affichage du temps et les 3 boutons.
 * 2. Créer les variables pour suivre l'état : 
 *    - `tempsEcoule` (entier, en secondes ou millisecondes) 
 *    - `intervalId` (pour stocker l'ID du setInterval et pouvoir l'arrêter).
 * 3. Créer une fonction qui met à jour le texte du div `#affichage` (ex: "00:01:23").
 * 4. Ajouter les événements `click` sur les boutons :
 *    - Démarrer : Lance un setInterval qui incrémente le temps et met à jour l'affichage. (Désactive ce bouton et active "Pause").
 *    - Pause : Arrête le setInterval avec clearInterval. (Désactive ce bouton et réactive "Démarrer").
 *    - Réinitialiser : Arrête le chrono, remet le temps à 0, met à jour l'affichage, et réinitialise l'état des boutons.
 * 
 * BONUS : 
 * - Formater le temps pour toujours afficher 2 chiffres (00:05:09 au lieu de 0:5:9).
 *   Indice : Regarde la méthode de chaîne `.padStart(2, '0')`.
 */

// --- Ton code ci-dessous ---

// 1. Sélection des éléments


// 2. Variables d'état


// 3. Fonctions utiles


// 4. Écouteurs d'événements

