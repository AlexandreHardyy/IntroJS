/**
 * Exercice : Gérer une erreur avec try/catch
 * Consigne : La fonction `parserJSON` reçoit une chaîne de caractères et doit la parser en objet JS.
 * Si la chaîne n'est pas du JSON valide, la fonction `JSON.parse` va générer une erreur et faire planter le programme.
 * Utilisez un bloc try...catch pour :
 * 1. Retourner l'objet parsé si tout va bien.
 * 2. Retourner null et afficher une erreur (console.error) si le parsing échoue.
 */

export function parserJSON(chaine) {
    // Écrivez votre code ici
}
