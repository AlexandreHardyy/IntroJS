/**
 * Exercice : Lancer une erreur personnalisée
 * Consigne : La fonction `retirerArgent` prend un solde initial et un montant à retirer.
 * 1. Si le `montant` est supérieur au `soldeInitial`, levez d'une erreur en utilisant : throw new Error("Solde insuffisant").
 * 2. Si le montant est négatif, levez une erreur : throw new Error("Montant invalide").
 * 3. Sinon, retournez le nouveau solde restant.
 */

export function retirerArgent(soldeInitial, montant) {
    // Écrivez votre code ici
}
