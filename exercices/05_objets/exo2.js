/**
 * Exercice 2 : Accès propriétés
 * Consigne : Créez une fonction `afficherInfos` qui prend un objet personne (avec nom et age).
 * Elle retourne une chaîne : "Nom: [nom], Age: [age]".
 */

export function afficherInfos(personne) {
    // Écrivez votre code ici
    return "";
}
