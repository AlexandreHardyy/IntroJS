/**
 * Exercice 3 : Logique Objet
 * Consigne : Créez une fonction `estAdulte` qui prend un objet personne.
 * Elle retourne `true` si la personne a 18 ans ou plus, `false` sinon.
 */

export function estAdulte(personne) {
    // Écrivez votre code ici
    return false;
}
